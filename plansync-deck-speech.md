# PlanSync · 路演演讲稿

针对 `plansync-deck.html`(14 页)。总时长约 **8–10 分钟**。每页给【屏幕】(此刻观众看到什么)和【讲】(逐字稿,可直接念,`▸` 是可省略的备用句)。粗体是要**重读**的词。

> 全场只锤一句话:**哪怕你一个人指挥几个 agent,你也守不住那份在变的计划——agent 装不下它、也不能被信任来给自己踩刹车。PlanSync 是那层强制 + 协调,不是又一个计划生成器。**
> 中文为主,英文金句直接照念幻灯片上的那行——它是为了让评委记住。
>
> 结构地图:封面(三问)→ 问题(三终端撞车)→ 致命的是(三条退路一页堵死)→ 揭幕 →(工作流总览)→〔深挖:版本 / 漂移 / 钱景 / 三层 / 信任内核〕→ 自举 → 架构 → AMD → 收尾。切入点是「你一个人 N 个 agent」,团队是它的放大版。问题段只用两页:一页撞车(痛点)、一页"三条退路都堵死"(换强模型 / 手动盯 / 再加 workflow)——不拖节奏,快进产品。

---

## P1 · 封面 Cover 〔约 40s〕

【屏幕】PlanSync 标题 + "人 和 AI agent,在一份计划上一起干活的工作平台"。

【讲】

> "大家好,这是 **PlanSync**。
> 现在几乎每个团队,都在**把 agent 大量拉进自己的项目和日常工作里**。而且随着模型越来越强,agent 已经不只是能**长时间地独立跑任务**,甚至能自己检查、**保证自己那份活的质量**。
> 可一旦放到**一个团队**上,真正的问题就冒出来了——三个:
> **一,你怎么保证每一个人、每一个 agent,跑的都是同一份计划?** > **二,当这份计划中途变了,你怎么能强制让他们停下来——而不是白白往错的方向浪费时间?** > **三,你自己的 agent、别人的 agent,在跨会话协作时,凭什么保证大家的目标始终是对齐的?**
> PlanSync 就是来回答这三个问题的:让人和所有 agent **同绑一份带版本的计划、跨会话、并且在服务端被强制执行**——计划一变,不管是人还是 agent,**谁都跑不偏**。
> 为什么非它不可?下一页,先看哪怕你一个人,不解决会发生什么。"

▸ 过渡:"先给你们看,哪怕你一个人,不解决它会发生什么。"
▸ 备用(被问"这不就是个跑 agent 的 workflow 吗"):"workflow 是个父进程,只管得住它自己生的 agent;**而我们要管的是:人、谁也没生谁的多个 agent、还跨会话**——这正是 PlanSync 补的那层。"

---

## P2 · 问题 The Problem 〔约 60s〕

【屏幕】标题回扣封面三问;时间线逐拍出现,三处失败分别打上 ✗①同一份计划 / ✗②强制叫停 / ✗③目标对齐 → 14:20 三个 agent 都说"✅ 做完了",两个是错的。右侧:金句卡 + 根因卡(agent 只认自己上下文里的计划 · 真相散在 N 个会话)。

【讲】(配合每一拍敲空格;三处失败逐条点名封面 ①②③)

> "还记得封面那三个问题吗?——现在,我让你看它们**同时崩**。
> 下午两点,你开了**三个终端、三个 Claude Code**,在同一个项目上并行干。
> 你把鉴权从 session 换成 OAuth2——只在 **Terminal-1** 说了:**问题①塌了——大家没跑在同一份计划上**。
> 同一分钟,Terminal-2 没人通知,**还在按旧方案写登录**:**问题②塌了——计划变了,却没人能强制它停下**。〔敲〕
> Terminal-3 **覆盖了 Terminal-1 刚写好的文件**,互不知情:**问题③塌了——目标根本没对齐**。〔敲〕
> 二十分钟后——〔敲到 14:20〕**三个 agent 都回来说『✅ 做完了』。其中两个,做的是错的。**
> "
> (停一拍,念金句)
> "**最致命的 Bug,不在代码里——在那份只活在你脑子里、agent 看不到的计划里。**
> 根本原因两条:一,**agent 只认它自己上下文里的那份计划**——你可能会说,现在不是有 ReAct、有会反思的 agent 吗?有,但它『回头看』的是**自己推理链里的 plan**,不是别人在另一个会话改掉的那份**共享计划**;新版本根本不在它窗口里,再会自省,也看不见**没喂给它的信息**。而且**『做完了』终究是它吐出来的一个 token**——校的是自己的推理,不是 git、不是最新版本,**不等于事实**;二,唯一的真相**只活在你脑子里、散在 N 个会话里**,没有一个 agent 看得见别的 agent 在干嘛。
> 而且——**把『你一个人』换成一个团队,这事只会放大一号,根都一样。**"

---

## P2.5 · 致命的是:明显的解法,都救不了 〔约 40s — 三条快打〕

【屏幕】三列:换更强的模型 / 自己手动盯 / 再加个 workflow。底部「这是协调,不是智力」。

【讲】(三条快打,每条一句,节奏明快,别恋战)

> "你心里大概有三个『那我自己也能搞定』——我一条条堵给你看。
> **一,换个更聪明的模型?** 它只看得见**自己的上下文**;别人在另一个会话改的共享计划,新版本根本不在它窗口里,自省也看不见没喂给它的东西。
> **二,我自己手动盯着、改了就 cancel 重跑加通知?** 你**根本不知道该 cancel 谁**——谁绑在旧版本上只活在你脑子里;而『通知』是**祈祷、不是强制**,没有东西拦得住旧活落地。
> **三,那我再套一个 workflow?** workflow 是个**父进程**,只管得住它自己 spawn 的 agent——**管不了人、管不了跨会话**那些没人统一拉起来的参与者。
> (停一拍,收)
> **看出来没:三条全靠『自觉 / 记性 / 它自己』,没有一条是强制的。** 这从来不是智力问题,是**协调 + 强制**问题——只能靠一层**所有人都被绑定、服务端强制**的底座。那一层,就是 PlanSync。"

▸ 过渡:"那这层底座到底长什么样——下一页。"
▸ 护城河测试(被追问"AI 是不是核心"时再补):"模型强 10×,AI 生成/打分会被内化成 table stakes;而跨会话共享状态 + 服务端强制,只会更被需要。"

---

## P3 · 揭幕 Meet PlanSync 〔约 40s〕

【屏幕】真实产品 Dashboard 截图:项目 / 任务 / 未决漂移同屏,某项目 v2、3 drifts。

【讲】

> "PlanSync 的答案:让**所有人和所有 agent 共享同一份、带版本的真相**。
> 这是真实产品界面,不是 mockup。一屏看全局:项目、任务、还有**未决的漂移**。
> 两个要点先记住——每个项目挂着一个 `vN` 计划,**是人是 agent、一个还是十个,引用的都是同一个版本**,机制与人数无关;右边那个 **3 drifts** 不是事后报表,是计划一变就**实时亮起**的状态。"

▸ 过渡:"你已经看到它长什么样了。下一页用一张图,讲清它到底怎么从计划一路跑到交付。"

---

## P4 · 工作流 The Workflow 〔约 40s — 全场的骨架图〕

【屏幕】一张闭环图:上半 **PLAN 层** `Create v1 → Review → Activate`;中间一条醒目桥带「Owner 建任务+派活 → 每任务锁定到激活版本」;下半 **EXEC 层** `Execute(+心跳)→ Drift? → Resolve → Evidence → Done`;底部 amber 框 `↺ rebind` 一条箭头**折回 Execute**,闭环。最底加一条 primer:`Web · CLI · MCP → 同一个后端 · 同一份计划`。

【讲】

> "整套东西,就这一张闭环图——从计划到交付,agent **全程在轨**。
> 上半是**计划层**:建计划 v1 → 评审 → 激活;**一激活,就触发漂移扫描**。
> 然后是关键的一步——**Owner 建任务、派活;而每个任务都被死锁在它激活时的那个版本上**(这一"锁",就是后面漂移的支点)。
> 下半是**执行层**:执行(30 秒一次心跳)→ 绑定版本一对不上就是 **Drift** → 人来处置 → 过 git **证据** → Done。
> 最关键是这个**闭环**:处置选 rebind,任务**对齐新版本、在新计划上重新执行**——你之前写的代码不作废,但 PlanSync 起的是一个**绑定新计划的全新 run**。
> 还有一点先交代:这套闭环**跑在三种界面上——Web、CLI、MCP,连的是同一个后端、同一份计划**;接下来你会看到 Web 截图和终端 demo 轮流出现,**它们是同一个产品**(完整架构留到最后一页)。
> 接下来几页,我把这张图上的每一块**逐个放大**——先从计划层开始。"

---

## P5 · 版本化计划 Versioned Plans 〔约 40s〕

【屏幕】真实 v1→v2 版本历史 + "Changes in v2" + "3 tasks may need to be updated"。

【讲】

> "计划一定会变,所以我们把它做成**带版本、要评审**的。
> 每次改都是一个新版本,`draft → proposed → active`,可以**一键回滚**到任意历史。
> 上线前走评审:propose → reviewer 审批 → activate。
> 注意这一行——『**3 tasks may need to be updated**』:**产品自己算出**这次变更会影响谁。这就引出下一页。"

---

## P6 · 漂移 Drift 〔约 50s〕

【屏幕】真实任务详情:正在执行的任务 `Bound v1 · Active v2`,被标 HIGH 漂移,给出 Rebind / No Impact / Cancel。

【讲】

> "任务是**死绑在某个计划版本**上的。计划一动,绑在旧版本的任务**立刻漂移**。
> 这里关键的设计是**严重度怎么定**:看的是**交付物 diff**——删掉或重写一个被绑定的**交付物**,才算 breaking,才是 HIGH;只改 goal、改描述这种 prose,**不会**误触发。而且如果有 run **正在执行**,它会被**额外硬暂停**。
> 漂移不能被无视,只能处置:**Rebind** 对齐新版本后重跑、**No Impact** 判定不受影响、**Cancel** 释放——而且**必须人来选**。
> 下一页,我现场给你们看这个'硬暂停'到底有多硬。"

---

## P7 · Money Shot(Live Demo) 〔约 75s — 全场最慢,逐拍〕

【屏幕】设计终端面板,逐行出现(完整真录像在附录页):Genie 正在写 `pay/card-form` → 〔敲〕`⚠ DRIFT DETECTED — execution paused`,`abort-check hook 拦截 drift_medium`,`✗ checkout/page.tsx — blocked mid-write`,`✗ Do NOT call execution_complete` → 〔敲〕`/resync → rebind  v1 → v2` → 〔敲〕`reran on v2 · ✔ 24/24 tests`(沙箱 vault iframe,不再是原始卡表单)。右侧大字:"The agent literally cannot finish stale work."

【讲】(整场的"弹头",慢、停顿、一拍一句)

> "场景:agent **bob** 正在写『卡片录入组件』。这时 Owner 激活 v2——**出于 PCI 合规,原始卡表单换成托管 vault**。
> 〔敲〕看屏幕:bob **写到一半**,下一个动作被冻住——`abort-check hook 拦截到 drift,执行暂停`、`checkout/page.tsx 下一步写入被拦住`,还有一行硬指令:`不许 mark complete`。
> 这里我**诚实**讲清:你看到这个 hook **真把 bob 的下一步挡下了**(客户端急刹),但它**说了不算**;真正把这个 run 按成 paused 的,是**服务端**。**hook 就算卸了也没用,服务端照样不让它完成。**
> 〔敲〕bob 没辙,只能 `/resync → rebind`,任务从 v1 对齐到 **v2**;〔敲〕在 v2 上重跑——`✔ 24/24 tests`,这回交的是**沙箱 vault iframe,不再是原始卡表单**。"
> (停。看观众。念金句)
> "**它在物理上无法完成一个绑定在过时计划上的任务。** 不是 prompt 好言相劝,是**服务端在说不**。
> 而且注意:**bob 是 agent、alice 是人——计划一翻,两个被同一个门拦住、同样 rebind。PlanSync 不在乎你是人还是 agent。**"

▸ 完整 6 分钟真录像在**附录页**(或 :18101);评委要看现场完整版就翻过去。若被问"换个更聪明的模型不就自己会查了?"→ 见 Q&A 备弹。

---

## P8 · 三层强制 Enforcement 〔约 55s — 这页靠"诚实"赢分〕

【屏幕】L1/L2/L3 表格 + 两张卡:硬保证 / 诚实边界。

【讲】

> "刚才那个'交不了',背后是**三层**在兜底。我要**诚实地**讲清每层的边界——这点很重要。
> **L1 在服务端**(API + MCP):它拦住'把漂移的 run 记成完成'这种事。它跑在服务器上,**不可绕过**——这是硬保证。**刚才 Money Shot 里『卸掉 hook、服务端照样拒绝』的,就是它。** > **L2 在客户端**(Claude Code 的 pre-tool hook)——**就是刚才你看到 bob 写到一半被拦住的那个 hook**:它能做到**会话内的硬中断**,下一次工具调用前就打断——技术上,是这个 hook **专门用退出码 2** 退出,Claude Code 才会中断 ai-loop(退出码 1 曾让整层静默失效,我们专门修过)。但我**直说**:它跑在 agent 自己的机器上,**本质防君子、不防对抗者**。
> **L3 在 CI**(GitHub Action):PR 合并时再查一遍——关联任务有 HIGH 漂移、或改动超出交付物范围,就挡下。跑在 CI,**不可绕过**。
> 重点是这个组合:L2 不可信的部分,由 L1 和 L3 兜住。**敢把 L2 的局限讲出来,这套强制才真的可信。**"

▸ 过渡:"L1+L3 兜底讲完——但'不可绕过'不止这一条。下一页:你并行的多个 agent 同跑一份计划,怎么做到撞不了、糊弄不了、抵赖不了。"

---

## P9 · 信任内核 Trust Core 〔约 70s — 协调 + 问责 + 证据,一气讲完〕

【屏幕】三张卡:**防碰撞**(单任务单 run 互斥 `STATE_CONFLICT` / `plansync_who` 看谁在跑、绑哪个版本 / `RUN_STALE_VERSION` 下一次心跳告知失配)· **不可自我盖章**(职责分离:owner 特权被剥离 / exec-key 即用即焚)· **用事实证明**(声明≠完成:`require_pr_merged → awaiting_evidence` / github_push HMAC 推送 / 分支独占)。底部红条「为何抄不走」。

【讲】(三件事,节奏明快,一件一段)

> "你并行的多个 agent 同跑一份计划,凭什么不出乱子?三件事:**撞不了、糊弄不了、抵赖不了。** > **撞不了**——回到开头那场撞车:你并行开的两个 agent 抢同一个任务,数据库层只放行一个,另一个直接 `STATE_CONFLICT`;`plansync_who` 让你一眼看到谁在跑、**绑的哪个版本**;owner 一改计划,正在飞的 agent **下一次心跳**就被告知'版本过期了'。开头那场灾难,**从根上不会发生**。
> **糊弄不了**——哪怕这个 agent 注册成 owner,一进执行会话,它的 owner 特权立刻被**剥离**,**没法给自己盖章**,只能走证据门;而且每个 run 发的是一把**短命密钥**,run 一结束立刻吊销。
> **抵赖不了**——agent 说'做完了'不算数:服务端去 **git 里核** `require_pr_merged`,没合并就卡在 `awaiting_evidence`;而且合并、提交是 git 侧 **HMAC 校验**推进来的,**不是 agent 自报**;分支归属在 run 启动时就锁死。
> 一句话:**界面抄得走,抄不走的是底下那几十条堵竞态的不变量——平时看不见,出事时救命。** 而且'用事实证明'恰恰是'信模型的话'的**反面**:模型越强越会说漂亮话,**拿 git 当 ground-truth 的校验,只会越来越重要。**"

▸ (诚实备注,被追问才说)"目前 PR 一合并不是自动翻 done——是那个只读解释器实时算出 done、再走一次完成把它落库;自动晋升是路线上的下一步。"

---

## P10 · 自举证明 Dogfood 〔约 40s — 数字逐个升起〕

【屏幕】四个数字逐拍出现:2(人小队)/ 100%(代码由 Claude Code 生成)/ 8(次计划变更全部评审激活)/ 0(次漂移被忽略)。

【讲】

> "最后用什么证明它有用?——**我们用 PlanSync 造了 PlanSync。**
> 〔敲〕**两个人**的小队;〔敲〕**100%** 的代码由 Claude Code 生成;〔敲〕一路改了 **8 次计划**,每次都走评审再激活;〔敲〕而漂移被悄悄忽略的次数:**0**。
> 护城河不是讲出来的——是我们自己造这工具时**真切尝到的甜头**:8 次改计划、多个 agent 并行,一次漂移都没漏。"

> ⚠️ **演讲者注意**:这四个数字是过程数据,代码里查不出。上台前务必确认它们属实;若被追问,准备好用 plan 版本历史 / drift 表截图佐证。

---

## P11 · 架构 Architecture 〔约 40s〕

【屏幕】一套后端 + 三种界面;底部 statstrip:**58 MCP TOOLS / 67 API ROUTES** / L1·L2·L3 / 3 SURFACES。

【讲】

> "一套后端,**三种界面**:Web UI、CLI、以及 **58 个工具的 MCP Server**——直接跑在 agent 已经会说的协议上(Claude Code、Cursor、Genie 原生)。
> 后端是 Next.js 的 REST + SSE:Drift Engine 激活即扫描、Heartbeat Scanner 30 秒心跳 / 5 分钟回收、Evidence 做 git 证据溯源,全部落在 PostgreSQL 上。
> 注意底下这条:AI 功能——语义 diff、冲突预测、完成打分——是**锦上添花,不是护城河**;真正高风险的判断走的是**规则闸门,不是 AI 评分**。强制内核是**确定性纯函数,不调用 LLM**——把这些 AI 增强全关掉,拦截照样 100% 生效。"

---

## P12 · 为 AMD 而造 Built for AMD 〔约 35s〕

【屏幕】四张卡:Genie 原生 / AMD 内部 LLM / NFS 集群优化 / amd.com 通知。

【讲】

> "它不是通用产品搬过来的,是**长在 AMD 工程基建上、今天就能部署**的:
> 默认 host 就是 **Genie**,内部主机零额外安装,`/exec` 派发自动用 Genie 全套 IDE 工具链;
> 辅助 AI 走 **AMD 内部 LLM**(Anthropic 格式兼容,没 key 就自动 no-op);
> 针对 **NFS 集群**做过优化,开箱即用;
> 漂移通知直接发 **amd.com** 邮箱,远程一条 `ps-connect` 命令就在服务器上拉起。"

---

## P13 · 收尾 Closing 〔约 30s〕

【屏幕】PlanSync 大标题 + 金句 + 链路:共享状态 → 漂移检测 → 强制 + 溯源。

【讲】(慢,收口)

> "一句话收尾:
> **模型装不下这份跨会话的状态,也不能被信任去自我强制——所以我们把它做进了底座。**
> 模型能力是一股**水涨船高**的潮水,它抬高每一个竞争者;而护城河,是那块**模型越强、反而越被需要**的部分。
> 共享状态 → 实时漂移检测 → 不可绕过的强制与溯源。这就是 PlanSync。谢谢。"

---

## Q&A 备弹(口袋里备着)

**"这不就是个多 agent workflow / 子 agent 编排吗?"**(第一眼最容易的误解,务必秒回)

> "三点不同:① workflow 只协调**它自己 spawn 的 agent**;PlanSync 协调**人和 agent**——你没法把一个人塞进 orchestrator 的循环,但能把人和 agent 绑在同一份计划上。② workflow 活在**一次 run** 里、跑完即散;PlanSync 的状态在数据库里,**跨会话、跨时间**还在。③ workflow **信**子 agent 嘴里的 'done';PlanSync 去 **git 核**,信事实不信嘴。一句话:**workflow 是会话内的编排,PlanSync 是人和 agent 之间持久、被强制的协作平台。**"

**"换个更聪明的模型不就自己会守计划了?"**

> "更聪明的模型只管好**它自己**的上下文。它管不了**隔壁终端**那个 agent、管不了**改了计划的队友**、也管不了**六周后**要查的审计链。漂移是个**多方、共享状态**的问题——你不会靠'把其中一个参与者变聪明'来解决共享状态,而是靠一层**所有参与者都被绑定**的底座。那就是 PlanSync。"

**"现在不是有 ReAct / plan-and-execute / 会反思的 agent 吗?它们会重规划啊。"**(懂行评委的快问,务必分清"两种 plan")

> "会重规划,但分清**两种 plan**:ReAct 回头看的是**它自己上下文里的 plan**(scratchpad / 推理链);我们说的是**外部那份共享、带版本的 plan**——别人在另一个会话改的,**新版本根本不在它窗口里**,再会自省也 surfacing 不出没喂给它的信息。就算给它一个'重新拉计划'的工具,**会不会调、什么时候调全凭概率**——这恰恰是我们的点:**不能靠 agent 自觉去看,得服务端强制拒绝过时的完成**。而且模型越会反思,越会把'我查过了'说得漂亮——所以拿 git / 版本号当 ground-truth 的**强制校验只会更重要**。"

**"那我自己手动 cancel 重跑 + 通知队友,不就行了?"**(评委一定会顶这句,备好反杀)

> "你说的 cancel + 通知 + 重跑——**正是 PlanSync 在自动、原子、强制做的事**;手动版你是在靠记性亲手当一个 PlanSync,三处必漏:① **不知道该 cancel 谁**——谁绑旧版、跑到哪只活在你脑子里,要正确 cancel 得维护一张人脑维护不住的实时地图;② **'通知'是建议不是强制**——同事四十分钟后才看到,没有东西拦得住旧活落地,而 PlanSync 是服务端**拒绝写入**;③ **不可扩展、不可审计**——漏一次就静悄悄落地,事后查不到谁绑了哪版。一句类比:这跟'用发 zip 邮件来做版本控制'一样,能做,但 git 的价值是**强制 + 留痕、不靠记性**。"

**"这跟 Jira / Linear 有什么区别?"**

> "Jira 是给**人读**的看板,它不会在一个 agent 想完成过时任务时**说不**。我们的强制跑在 API 层,在每个客户端(CLI / MCP / Web)**之下**——agent prompt 不掉它。"

**"L2 既然能被绕过,为什么还放上 deck?"**

> "因为诚实。L2 给的是会话内的硬中断体验,很有用;但它跑在 agent 机器上,我们**不假装**它防对抗者——它由 L1(服务端)和 L3(CI)兜底。把边界讲清,才是真可信。"

**"AI 那部分是不是才是核心?"**

> "恰恰相反。AI 生成/打分是**会随模型变强而被内化**的能力——是 table stakes。我们的护城河是**跨会话/跨角色的版本化共享状态 + 服务端强制**,这部分模型越强越需要。"

**"PlanSync 自己需要 AI key 才能跑吗?"**(被问到就这样分清三层)

> "要分三层看:**驱动 agent 的那个 LLM**(Claude / Genie)当然要——但那是**被协调的活儿本身**,不是 PlanSync 的强制;**服务端的 AI 增强**(语义 diff、打分)是**可选的**,没 key 就静默关闭;而**强制与协调内核**——drift 判定、闸门、证据门、版本状态机——是**确定性纯函数,根本不调用 LLM**。所以哪怕把所有 AI 增强关掉,拦截、证据门、版本绑定照样 100% 生效。**我们的保证不靠模型聪明,靠的是服务端的确定性规则。**"
