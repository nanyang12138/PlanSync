# PlanSync · 路演演讲稿

针对 `plansync-deck.html`(13 页)。总时长约 **8–10 分钟**。每页给【屏幕】(此刻观众看到什么)和【讲】(逐字稿,可直接念,`▸` 是可省略的备用句)。粗体是要**重读**的词。

> 全场只锤一句话:**模型越来越会"生成"计划;它们依然不擅长在多人、多 agent、跨会话并行时"守住"一份计划。PlanSync 是那层强制 + 协调,不是又一个计划生成器。**
> 中文为主,英文金句直接照念幻灯片上的那行——它是为了让评委记住。

---

## P1 · 封面 Cover  〔约 40s〕

【屏幕】PlanSync 标题 + "多个 AI agent 之间唯一可信、可被强制的那份真相"。

【讲】
> "大家好,这是 **PlanSync**。
> 现在每个团队都在让 agent 干活——但注意,不是**一个** agent,是**好几个 agent、还和人一起、跨会话并行**地干。
> 问题来了:当计划在变、而没人盯着的时候,**谁来保证它们没在朝错误的方向狂奔?**
> 这就是 PlanSync 要解决的事——它是多个 agent 和人之间,**唯一可信、而且可以被强制执行**的那份真相。"

▸ 过渡:"先给你们看,不解决它会发生什么。"

---

## P2 · 问题 The Problem  〔约 60s〕

【屏幕】一条时间线逐条出现(空格逐拍):09:00 Owner 改鉴权方案 → 09:01 Alice 跟上 → 09:01/09:02 Bob、Dave 仍按旧方案写、**无通知** → 17:00 Merge day,冲突,工时归零。

【讲】(配合每一拍敲空格)
> "早上九点,Owner 改了 spec:鉴权从 session 换成 OAuth2。
> Agent-Alice 收到了,按新方案写。
> 但同一分钟,Agent-Bob **还在按旧方案写登录**——没人通知他。〔敲〕工程师 Dave 也是。〔敲〕
> 到傍晚 merge day——〔敲到 17:00〕**冲突。架构不兼容。一整天的工时归零。**
> "
> (停一拍,念金句)
> "**AI 协作里最致命的 bug,不在代码里——在那份没人知道已经过时的计划里。**
> 根本原因很简单:AI agent 是**无状态执行器**,它不会主动检查计划变没变;而 Jira、Notion 是给**人读**的,不是 agent 能消费、能被强制的协议。"

---

## P3 · 揭幕 Meet PlanSync  〔约 45s〕

【屏幕】真实产品 Dashboard 截图:项目 / 任务 / 未决漂移同屏,某项目 v2、3 drifts。

【讲】
> "PlanSync 的答案:让**所有人和所有 agent 共享同一份、带版本的真相**。
> 这是真实产品界面,不是 mockup。一屏看全局:项目、任务、还有**未决的漂移**。
> 两个要点先记住——每个项目挂着一个 `vN` 计划,人和 agent 引用的是**同一个版本**;右边那个 **3 drifts** 不是事后报表,是计划一变就**实时亮起**的状态。"

---

## P4 · 版本化计划 Versioned Plans  〔约 40s〕

【屏幕】真实 v1→v2 版本历史 + "Changes in v2" + "3 tasks may need to be updated"。

【讲】
> "计划一定会变,所以我们把它做成**带版本、要评审**的。
> 每次改都是一个新版本,`draft → proposed → active`,可以**一键回滚**到任意历史。
> 上线前走评审:propose → reviewer 审批 → activate。
> 注意这一行——『**3 tasks may need to be updated**』:**产品自己算出**这次变更会影响谁。这就引出下一页。"

---

## P5 · 漂移 Drift  〔约 50s〕

【屏幕】真实任务详情:正在执行的任务 `Bound v1 · Active v2`,被标 HIGH 漂移,给出 Rebind / No Impact / Cancel。

【讲】
> "任务是**死绑在某个计划版本**上的。计划一动,绑在旧版本的任务**立刻漂移**。
> 这里关键的设计是**严重度怎么定**:看的是**交付物 diff**——删掉或重写一个被绑定的**交付物**,才算 breaking,才是 HIGH;只改 goal、改描述这种 prose,**不会**误触发。而且如果有 run **正在执行**,它会被**额外硬暂停**。
> 漂移不能被无视,只能处置:**Rebind** 对齐新版本续跑、**No Impact** 判定不受影响、**Cancel** 释放——而且**必须人来选**。
> 下一页,我现场给你们看这个'硬暂停'到底有多硬。"

---

## P6 · Money Shot(Live Demo)  〔约 75s — 全场最慢,逐拍〕

【屏幕】终端:v2 激活(移除 bob 在做的 `pay/card-form`)→ drift high、run 自动暂停 → 〔敲〕bob 发心跳被拒 `RUN_PAUSED` → 〔敲〕bob 强行 complete 被拒 `RUN_PAUSED`。右侧大字:"The agent literally cannot finish stale work."

【讲】(这是整场的"弹头",慢、停顿、一拍一句)
> "场景:agent **bob** 正在做『原始信用卡表单』这个交付物。这时 Owner 激活 v2——**出于 PCI 合规,原始卡表单被禁止、整个删掉了**。
> 看引擎的反应:它把 bob 绑定的交付物和新版本一比,发现**被删了**,判 **high**;因为有 run 正在飞,bob 的 run **被自动暂停**——他什么都没做,是平台做的。
> 〔敲空格〕bob 的 agent 根本不知道计划变了,照常发下一个心跳——**被拒**:`RUN_PAUSED,a newer plan version superseded this run`。
> 〔敲空格〕然后是最关键的一下:bob **强行想把活儿标记成完成**——**拒绝**:`Cannot complete a paused run`。"
> (停。看观众。念金句)
> "**他在物理上无法完成一个绑定在过时计划上的任务。**
> 这不是 prompt 在好言相劝,是**服务端 API 在说不**。一句 system prompt 可以**告诉** agent 去复查计划,但它**拦不住**一个不复查的 agent——**这一层,拦得住。**"

▸ 若评委问"换个更聪明的模型不就自己会查了?"→ 见最后的 Q&A 备弹。

---

## P7 · 三层强制 Enforcement  〔约 55s — 这页靠"诚实"赢分〕

【屏幕】L1/L2/L3 表格 + 两张卡:硬保证 / 诚实边界。

【讲】
> "刚才那个'交不了',背后是**三层**在兜底。我要**诚实地**讲清每层的边界——这点很重要。
> **L1 在服务端**(API + MCP):它拦住'把漂移的 run 记成完成'这种事。它跑在服务器上,**不可绕过**——这是硬保证。
> **L2 在客户端**(Claude Code 的 pre-tool hook):它能做到**会话内的硬中断**,下一次工具调用前就打断——技术上,是这个 hook **专门用退出码 2** 退出,Claude Code 才会中断 ai-loop(退出码 1 曾让整层静默失效,我们专门修过)。但我**直说**:它跑在 agent 自己的机器上,**本质防君子、不防对抗者**。
> **L3 在 CI**(GitHub Action):PR 合并时再查一遍——关联任务有 HIGH 漂移、或改动超出交付物范围,就挡下。跑在 CI,**不可绕过**。
> 重点是这个组合:L2 不可信的部分,由 L1 和 L3 兜住。**敢把 L2 的局限讲出来,这套强制才真的可信。**"

---

## P8 · 协调 + 问责 Coordination & Accountability  〔约 45s〕

【屏幕】左栏「防碰撞」:单任务单 run 互斥 / plansync_who / RUN_STALE_VERSION;右栏「问责」:职责分离 · 不能自我盖章 / exec-key 即用即焚。底部红条回扣 P2 的撞车。

【讲】
> "回到开头那场撞车——它**为什么从根上不会发生**?
> 左边是**防碰撞**:两个 agent 抢同一个任务,数据库层只放行一个,另一个直接 `STATE_CONFLICT`;`plansync_who` 让你一眼看到谁在跑、**绑的哪个版本**;owner 一改计划,正在飞的 agent **下一次心跳**就被告知'版本过期了,重启'。
> 右边是**问责**,这点很关键:哪怕这个 agent 注册成 owner,一旦进了执行会话,它的 owner 特权就被**剥离**——**没法给自己盖章**,只能走证据门;而且每个 run 发的是一把**短命密钥**,run 一结束立刻吊销,会话关了它就无法在记录之外偷偷动手。
> 一句话:**抄得走界面,抄不走的是底下那几十条堵竞态的不变量。**"

▸ 过渡:"防碰撞讲完,下一页讲它的孪生兄弟——没证据,你说做完也不算。"

---

## P9 · 证据溯源 Strict Sourcing  〔约 60s — 第二记重拳,与 P6 同构〕

【屏幕】终端逐拍:agent 自报 complete → `✓ run completed`(活儿没丢)→ 〔敲〕`⚠ task → awaiting_evidence, missing:[pr_merged]` → 〔敲〕`GET completion-state → gateApplied:true prMerged:false` → 〔敲〕PR 合并后再查 `prMerged:true status:done`。

【讲】
> "P6 是'**过时的活你继续不了**';这一页是它的孪生兄弟:'**没证据,你说做完也不算**'。
> agent 报 done。服务端**不拦这个 run**——活儿不丢,run 照样 completed。
> 〔敲〕**但任务没有变 done,它落到了 `awaiting_evidence`**,告诉你缺什么:`missing: pr_merged`。'做完' ≠ '真完成'。
> 〔敲〕你随时可以只读地追问'为啥还没 done'——这就是那个解释端点:`gateApplied:true, prMerged:false`。
> 〔敲〕等那个 PR **真的合并**、commit 真能对应上交付物,再查一次——`prMerged:true, status:done`。**证据落地,才认。**
> 为什么这比'AI 打分'值钱?因为 agent 再聪明,也**没法靠声称让一个 PR 变成已合并**——真相在 git 里,由服务端去核。**模型越强,这种把'声明'和'事实'分开的校验,只会越重要。**"

▸ (诚实备注,被追问才说)"目前持久状态不是 PR 一合并就自动翻 done——是这个只读解释器实时算出 done、再走一次完成把它落库;自动晋升是路线上的下一步。"

---

## P10 · 自举证明 Dogfood  〔约 40s — 数字逐个升起〕

【屏幕】四个数字逐拍出现:2(人小队)/ 100%(代码由 Claude Code 生成)/ 8(次计划变更全部评审激活)/ 0(次漂移被忽略)。

【讲】
> "最后用什么证明它有用?——**我们用 PlanSync 造了 PlanSync。**
> 〔敲〕**两个人**的小队;〔敲〕**100%** 的代码由 Claude Code 生成;〔敲〕一路改了 **8 次计划**,每次都走评审再激活;〔敲〕而漂移被悄悄忽略的次数:**0**。
> 护城河不是讲出来的——是我们自己造这工具时**真切尝到的甜头**:8 次改计划、多个 agent 并行,一次漂移都没漏。"

> ⚠️ **演讲者注意**:这四个数字是过程数据,代码里查不出。上台前务必确认它们属实;若被追问,准备好用 plan 版本历史 / drift 表截图佐证。

---

## P11 · 架构 Architecture  〔约 40s〕

【屏幕】一套后端 + 三种界面;底部 statstrip:61 MCP TOOLS / 66 API ROUTES / L1·L2·L3 / 3 SURFACES。

【讲】
> "一套后端,**三种界面**:Web UI、CLI、以及 **58 个工具的 MCP Server**——直接跑在 agent 已经会说的协议上(Claude Code、Cursor、Genie 原生)。
> 后端是 Next.js 的 REST + SSE:Drift Engine 激活即扫描、Heartbeat Scanner 30 秒心跳 / 5 分钟回收、Evidence 做 git 证据溯源,全部落在 PostgreSQL 上。
> 注意底下这条:AI 功能——语义 diff、冲突预测、完成打分——是**锦上添花,不是护城河**;真正高风险的判断走的是**规则闸门,不是 AI 评分**。强制内核是**确定性纯函数,不调用 LLM**——把这些 AI 增强全关掉,拦截照样 100% 生效。"

---

## P12 · 为 AMD 而造 Built for AMD  〔约 35s〕

【屏幕】四张卡:Genie 原生 / AMD 内部 LLM / NFS 集群优化 / amd.com 通知。

【讲】
> "它不是通用产品搬过来的,是**从 AMD 的工程基础设施上长出来的**:
> 默认 host 就是 **Genie**,内部主机零额外安装,`/exec` 派发自动用 Genie 全套 IDE 工具链;
> 辅助 AI 走 **AMD 内部 LLM**(Anthropic 格式兼容,没 key 就自动 no-op);
> 针对 **NFS 集群**做过优化,开箱即用;
> 漂移通知直接发 **amd.com** 邮箱,远程一条 `ps-connect` 命令就在服务器上拉起。"

---

## P13 · 收尾 Closing  〔约 30s〕

【屏幕】PlanSync 大标题 + 金句 + 链路:共享状态 → 漂移检测 → 强制 + 溯源。

【讲】(慢,收口)
> "一句话收尾:
> **模型装不下这份跨会话的状态,也不能被信任去自我强制——所以我们把它做进了底座。**
> 模型能力是一股**水涨船高**的潮水,它抬高每一个竞争者;而护城河,是那块**模型越强、反而越被需要**的部分。
> 共享状态 → 实时漂移检测 → 不可绕过的强制与溯源。这就是 PlanSync。谢谢。"

---

## Q&A 备弹(口袋里备着)

**"换个更聪明的模型不就自己会守计划了?"**
> "更聪明的模型只管好**它自己**的上下文。它管不了**隔壁终端**那个 agent、管不了**改了计划的队友**、也管不了**六周后**要查的审计链。漂移是个**多方、共享状态**的问题——你不会靠'把其中一个参与者变聪明'来解决共享状态,而是靠一层**所有参与者都被绑定**的底座。那就是 PlanSync。"

**"这跟 Jira / Linear 有什么区别?"**
> "Jira 是给**人读**的看板,它不会在一个 agent 想完成过时任务时**说不**。我们的强制跑在 API 层,在每个客户端(CLI / MCP / Web)**之下**——agent prompt 不掉它。"

**"L2 既然能被绕过,为什么还放上 deck?"**
> "因为诚实。L2 给的是会话内的硬中断体验,很有用;但它跑在 agent 机器上,我们**不假装**它防对抗者——它由 L1(服务端)和 L3(CI)兜底。把边界讲清,才是真可信。"

**"AI 那部分是不是才是核心?"**
> "恰恰相反。AI 生成/打分是**会随模型变强而被内化**的能力——是 table stakes。我们的护城河是**跨会话/跨角色的版本化共享状态 + 服务端强制**,这部分模型越强越需要。"

**"PlanSync 自己需要 AI key 才能跑吗?"**(被问到就这样分清三层)
> "要分三层看:**驱动 agent 的那个 LLM**(Claude / Genie)当然要——但那是**被协调的活儿本身**,不是 PlanSync 的强制;**服务端的 AI 增强**(语义 diff、打分)是**可选的**,没 key 就静默关闭;而**强制与协调内核**——drift 判定、闸门、证据门、版本状态机——是**确定性纯函数,根本不调用 LLM**。所以哪怕把所有 AI 增强关掉,拦截、证据门、版本绑定照样 100% 生效。**我们的保证不靠模型聪明,靠的是服务端的确定性规则。**"
